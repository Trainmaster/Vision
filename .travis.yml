language: php

php:
  - 7.1
  - 7.2
  - 7.3
  - master

matrix:
  allow_failures:
      - php: master

sudo: false

install:
  - composer install

script:
  - ./vendor/bin/phpunit

notifications:
  email: false
